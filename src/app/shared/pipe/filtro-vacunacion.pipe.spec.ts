import { FiltroVacunacionPipe } from './filtro-vacunacion.pipe';

describe('FiltroVacunacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroVacunacionPipe();
    expect(pipe).toBeTruthy();
  });
});
